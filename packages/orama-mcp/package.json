{
  "name": "@quebi/orama-mcp",
  "version": "0.0.0",
  "description": "stdio MCP server that indexes Markdown and JSONL files in the workspace with Orama (in-memory hybrid search).",
  "license": "MIT",
  "type": "module",
  "bin": {
    "orama-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --target bun --external onnxruntime-node --external onnxruntime-common --external sharp --external @huggingface/transformers",
    "start": "bun run ./src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@modelcontextprotocol/sdk": "1.20.0",
    "@orama/orama": "^3.1.18",
    "chokidar": "^5.0.0",
    "onnxruntime-common": "1.24.3",
    "onnxruntime-node": "1.24.3",
    "picomatch": "^4.0.5",
    "unpdf": "^1.6.2",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/picomatch": "^4.0.3",
    "typescript": "^5.9.0"
  }
}
